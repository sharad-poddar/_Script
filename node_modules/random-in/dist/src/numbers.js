"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNDigit = (num) => {
    return Math.floor(Math.pow(10, num - 1) +
        Math.random() * (Math.pow(10, num) - Math.pow(10, num - 1) - 1));
};
exports.getRandomBetween = (min, max) => {
    return Math.floor(Math.random() * (max - min + 1) + min);
};
exports.getRandomFloatBetween = (min, max) => {
    return Math.random() * (max - min + 1) + min;
};
exports.getNRandomBetween = (n, min, max) => {
    let numbers = new Array();
    while (numbers.length < n) {
        let currentNum = exports.getRandomBetween(min, max);
        if (!numbers.includes(currentNum)) {
            numbers.push(currentNum);
        }
    }
    return numbers;
};
